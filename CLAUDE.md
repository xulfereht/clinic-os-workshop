# ClinicOS workshop bootstrap

Read `AGENTS.md` as the shared bootstrap and runtime-routing source of truth.

The existing Claude Code + Codespaces/macOS/WSL track remains the `classic` default. Do not set
or infer a Codex profile for an existing classic installation. After the HQ starter is installed,
replace this bootstrap context with the starter's full `CLAUDE.md` and continue from its
first-contact workflow.
